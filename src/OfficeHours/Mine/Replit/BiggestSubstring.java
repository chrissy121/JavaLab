package OfficeHours.Mine.Replit;

public class BiggestSubstring {
    public static void main(String[] args) {
        String str = "aaabbbbcccccceeeeeeeeeeeeeeeeee";
        String temp = "";
        for (int i=0; i<str.length()-1; i++) {
            temp += str.charAt(i);
         if (str.charAt(i)!= str.charAt(i+1) || i==str.charAt(str.length()-2)) {

         }
        }
    }
}
